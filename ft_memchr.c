#include <string.h>

void *memchr(const void *s, int c, size_t n);
{

}

/*DESCRIPTION*/
	   /*The memchr() function scans the initial n bytes of the memory area pointed to by s for the first*/
	   /*instance of c.  Both c and the bytes of the memory area pointed to by s are interpreted  as  un‐*/
	   /*signed char.*/

/*RETURN VALUE*/
	   /*The memchr() and memrchr() functions return a pointer to the matching byte or NULL if the  char‐*/
	   /*acter does not occur in the given memory area.*/

